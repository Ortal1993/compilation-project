function func(a:Array<number>, b:Array<number>, c:Array<number>, d:Array<number>, e:Array<number>, f:Array<number>) {  
    if (true) {
        e.shift();
        return;
    }
    a.push(1);
    while (true) {
        b.pop();
        while (true) {
            c.unshift(2);
        }
        d.shift()
    }
    e.pop();
    while (true) {
        let x = 0;
    }
}
